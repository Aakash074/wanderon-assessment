# Production Environment Variables for Backend
# Copy this file to .env and update the values for production deployment

# Database Configuration
MONGODB_URI=mongodb+srv://...

# JWT Configuration
JWT_SECRET=your-super-secure-jwt-secret-key-change-in-production-CHANGE-THIS
JWT_EXPIRE=7d

# Cookie Configuration  
COOKIE_EXPIRE=7

# Security Configuration
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_TIME=15

# Application Configuration
PORT=10000
NODE_ENV=production

# CORS Configuration (Update with your actual frontend URL)
CLIENT_URL=https://your-netlify-app.netlify.app

# Example for custom domain:
# CLIENT_URL=https://your-custom-domain.com

# IMPORTANT SECURITY NOTES:
# 1. Change JWT_SECRET to a strong, unique value (minimum 32 characters)
# 2. Update CLIENT_URL with your actual frontend domain
# 3. Never commit this file with real values to version control
# 4. Use Render's environment variable dashboard for production values
